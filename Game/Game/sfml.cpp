#include <SFML/Graphics.hpp>

#include <MainMenu.h>

using namespace sf;

void main() {

	RenderWindow Menu(VideoMode(1080, 720), "Main Menu", Style:Default);

	MainMenu mainMenu(MENU.getSize().x, MENU.getsize().y);

	while (MENU.isOpen()) {
		EVent 
	}

}